<% @users.each do |user| %>
	<%= form_for :project, url: "/projects/#{@project.id}/add_members" do |f| %>
				<%= f.hidden_field :user_id, :value => user.id %>
				<%=  user.username %>
				<%= f.submit 'add' %>
	<% end %>
	<br/>
<%end %>